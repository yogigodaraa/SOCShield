# SOCShield Backend Dependencies (Optimized)
# Only includes packages actually used in the codebase

# Core API Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # PostgreSQL adapter (production)
alembic==1.12.1
asyncpg==0.29.0

# Redis & Task Queue
redis==5.0.1
celery==5.3.4
flower==2.0.1

# Email Processing
imapclient==2.3.1
email-validator==2.1.0
python-email==0.0.1
mailparser==3.15.0

# AI Providers (Choose one or more)
google-generativeai==0.3.1  # Gemini
openai==1.3.5                # GPT models
anthropic==0.7.4             # Claude

# Text Processing (Lightweight)
beautifulsoup4==4.12.2
lxml==4.9.3
regex==2023.10.3
tldextract==3.6.0  # For domain parsing in IOC extraction

# Security & Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.1

# HTTP & API Clients
httpx==0.25.2
aiohttp==3.9.1
requests==2.31.0

# Monitoring & Logging
prometheus-client==0.19.0

# Testing (Development)
pytest==9.0.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0
pytest-mock==3.14.0
aiosqlite==0.20.0  # SQLite for testing

# Note: The following libraries are NOT currently used in the codebase
# and can be safely removed to reduce installation size and time:
#
# REMOVED (Not used):
# - transformers==4.35.2  (1.2GB) - No Hugging Face models
# - torch==2.1.1          (800MB) - No PyTorch models
# - spacy==3.7.2          (80MB)  - No spaCy NLP
# - nltk==3.8.1           (50MB)  - No NLTK processing
#
# These were likely added for future features but are not implemented.
# Total space saved: ~2.1GB
